services:
  n8n:
    image: docker.n8n.io/n8nio/n8n:latest
    container_name: n8n
    ports:
      - "5678:5678"
    volumes:
      - ./data:/home/node/.n8n
    restart: unless-stopped
    environment:
      - TELEGRAM_CHAT_ID=${TELEGRAM_CHAT_ID}